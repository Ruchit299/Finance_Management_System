import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.company.finance',
  appName: 'Finance Tracker',
  webDir: 'dist/PFT_Management_System/browser'
};

export default config;
